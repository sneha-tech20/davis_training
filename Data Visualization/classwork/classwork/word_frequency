import string

with open("article.txt", "r") as f:
    text = f.read().lower()

for p in string.punctuation:
    text = text.replace(p, "")

words = text.split()
freq = {}

for w in words:
    freq[w] = freq.get(w, 0) + 1

print(freq)